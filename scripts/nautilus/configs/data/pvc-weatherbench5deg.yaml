# Input volume
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: weatherbench-5deg
spec:
  storageClassName: rook-cephfs
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 260Gi