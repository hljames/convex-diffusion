# @package _global_

# to execute this experiment run:
# python run.py experiment=oisst_full_cnn_simple

defaults:
  - oisst_full.yaml
  - override /model: cnn_simple.yaml
  - _self_

name: "OISSTv2-CNN"


logger:
  wandb:
    tags: ["oisstv2", "CNN"]
