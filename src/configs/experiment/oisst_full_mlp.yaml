# @package _global_

# to execute this experiment run:
# python run.py experiment=oisst

defaults:
  - oisst_full.yaml
  - override /model: mlp.yaml
  - _self_

name: "OISSTv2-MLP"


logger:
  wandb:
    tags: ["oisstv2", "mlp"]
